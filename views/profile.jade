extends layout

block content
  img(src=profile.gravatar+"&s=175").img-polaroid
  h1= title
  h2 Courses
  for course in profile.courses
    .course
      p= course.name
      h6= course.status
  h2 Posts
  for post in profile.posts
    .post
      p= post.message
      h6= post.course.name